library(tidyverse)
library(itdepends)
library(dplyr)

# needed to install
#install.packages("pkgcache")
#install.packages("cranlogs")

dep_usage_pkg("posterdown") %>% 
  count(pkg, sort = TRUE)

weights <- dep_weight("posterdown")
